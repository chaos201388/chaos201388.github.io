#!/usr/bin/env bash

# cd ~/Downloads
# wget -c https://repo.mysql.com/mysql-apt-config_0.8.14-1_all.deb
# sudo dpkg -i mysql-apt-config_0.8.14-1_all.deb

# sudo apt update
# sudo apt install mysql-server

# sudo mysql_secure_installation

# sudo systemctl status mysql.service

# mysql -u root -p -e "create database laravel6 character set utf8mb4 collate utf8mb4_unicode_ci;"
# mysql -u root -p -e "create user 'laravel6'@'localhost' identified by 'password';"
# mysql -u root -p -e "grant all privileges on laravel6.* to 'laravel6'@'localhost';"
